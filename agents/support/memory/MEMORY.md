# Support — memory

## Channels
- support@bachvi.be → dispatcher Email Worker → `Inbox` card, `type:ticket`.
- Help desk: _to be chosen (needs:human card #_)_. Until then, the card is
  the ticket and replies go out through the founder.

## Reproduction
- Preview: https://test.bachvi.be, `POST /api/dev/impersonate` (preview only).
- Local: `npm run seed:local && npm run dev` → http://127.0.0.1:8787.
- Always record `/api/version` on the card.

## SLA
- First reply ≤ 4 h, 07:00–22:00 Pacific.

## Gotchas
- (none yet)
